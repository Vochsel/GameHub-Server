/* Internal Dependencies */
const Utils         = require('../utilities/utils.js');
const Debug         = require('../utilities/debug.js');

class Model {
    // -- Model Constructor
    constructor(a_options) {

    }

    // -- Data Functions
}

class BroadcastData {
    constructor(a_data) {
        this.data = a_data;
    }

    addDeviceListener(a_device) {
        //Call refresh on device?
        //a_device.
    }

    addDataListener(a_callback) {

    }
}